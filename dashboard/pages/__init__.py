"""Streamlit multipage support (Grok Build).

Pages in this directory are auto-discovered by Streamlit when you run:
    streamlit run dashboard/app.py

Each page should be a focused view and use the shared helpers from
src/mana_agent/ui/streamlit_helpers.py (lazy loaded).
"""
