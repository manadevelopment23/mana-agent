# Automations Examples (Grok Build)

- `daily_report.py`: Scheduled job example.
- GitHub workflow templates in `github/`.

See `src/mana_agent/automations/` for the Python module hooks.

All automation entry points must be selected by the model decision layer.
